📦 Table: public.categories
  - created_at timestamp with time zone DEFAULT now()
  - description text
  - id integer NOT NULL DEFAULT nextval('categories_id_seq'::regclass)
  - image text
  - name character varying NOT NULL
  - parent_id integer
  - show_in_menu boolean DEFAULT false
  - sort_order integer DEFAULT 0
  - type text
  - updated_at timestamp with time zone DEFAULT now()

🔗 Foreign Keys:
  - categories_parent_id_fkey: parent_id → public.categories(id) ON DELETE SET NULL

🔑 Unique Constraints:
  - categories_pkey: id [PRIMARY KEY]

📊 Indexes:
  - categories_pkey: id [btree] [UNIQUE]

✅ Check Constraints:
  - categories_type_check: (type = ANY (ARRAY['collection'::text, 'sale'::text]))
  - type_check: (type = ANY (ARRAY['collection'::text, 'sale'::text]))

📦 Table: public.category_multi_items
  - category_id integer NOT NULL
  - color text
  - created_at timestamp with time zone DEFAULT now()
  - label text
  - sort_order integer DEFAULT 0
  - target_id integer NOT NULL
  - target_type text NOT NULL
  - updated_at timestamp with time zone DEFAULT now()

🔗 Foreign Keys:
  - category_multi_items_category_id_fkey: category_id → public.categories(id) ON DELETE CASCADE

🔑 Unique Constraints:
  - category_multi_items_pkey: (category_id, target_type, target_id) [PRIMARY KEY]

📊 Indexes:
  - category_multi_items_pkey: (category_id, target_type, target_id) [btree] [UNIQUE]

✅ Check Constraints:
  - category_multi_items_target_type_check: (target_type = ANY (ARRAY['product'::text, 'category'::text, 'sale_group'::text]))

📦 Table: public.category_sales
  - category_id integer NOT NULL
  - created_at timestamp with time zone DEFAULT now()
  - id integer NOT NULL DEFAULT nextval('category_sales_id_seq'::regclass)
  - quantity integer NOT NULL
  - sale_price numeric NOT NULL
  - updated_at timestamp with time zone DEFAULT now()

🔗 Foreign Keys:
  - category_sales_category_id_fkey: category_id → public.categories(id) ON DELETE CASCADE

🔑 Unique Constraints:
  - category_sales_pkey: id [PRIMARY KEY]

📊 Indexes:
  - category_sales_pkey: id [btree] [UNIQUE]

📦 Table: public.client_payments
  - amount numeric NOT NULL
  - client_id integer NOT NULL
  - created_at timestamp with time zone DEFAULT now()
  - id integer NOT NULL DEFAULT nextval('client_payments_id_seq'::regclass)
  - payment_method text NOT NULL
  - recorded_by text

🔗 Foreign Keys:
  - client_payments_client_id_fkey: client_id → public.clients(id) ON DELETE CASCADE

🔑 Unique Constraints:
  - client_payments_pkey: id [PRIMARY KEY]

📊 Indexes:
  - client_payments_pkey: id [btree] [UNIQUE]
  - idx_client_payments_client_id: client_id [btree]

✅ Check Constraints:
  - client_payments_amount_check: (amount >= 0::numeric)
  - client_payments_payment_method_check: (payment_method = ANY (ARRAY['credit'::text, 'paybox'::text, 'cash'::text]))

📦 Table: public.clients
  - address text
  - address_lat double precision
  - address_lng double precision
  - created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP
  - id integer NOT NULL DEFAULT nextval('clients_id_seq'::regclass)
  - manual_debt_adjustment numeric DEFAULT 0
  - name character varying
  - phone character varying
  - updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP

🔑 Unique Constraints:
  - clients_pkey: id [PRIMARY KEY]
  - unique_phone: phone [UNIQUE]

📊 Indexes:
  - clients_pkey: id [btree] [UNIQUE]
  - unique_phone: phone [btree] [UNIQUE]

📦 Table: public.order_items
  - created_at timestamp with time zone DEFAULT now()
  - group_bundle_qty integer
  - group_discount numeric NOT NULL DEFAULT 0
  - group_id integer
  - group_increment_step integer
  - group_sale_price numeric
  - group_unit_price numeric
  - id integer NOT NULL DEFAULT nextval('order_items_id_seq'::regclass)
  - in_stock boolean DEFAULT true
  - order_id integer NOT NULL
  - product_id integer
  - product_image text
  - product_name character varying
  - quantity integer NOT NULL
  - sale_price numeric
  - sale_quantity integer
  - unit_price numeric
  - updated_at timestamp with time zone DEFAULT now()

🔗 Foreign Keys:
  - order_items_order_id_fkey: order_id → public.orders(id) ON DELETE CASCADE
  - order_items_product_id_fkey: product_id → public.products(id) ON DELETE SET NULL

🔑 Unique Constraints:
  - order_items_pkey: id [PRIMARY KEY]

📊 Indexes:
  - idx_order_items_group_id: group_id [btree]
  - idx_order_items_order_id: order_id [btree]
  - order_items_pkey: id [btree] [UNIQUE]

✅ Check Constraints:
  - order_items_group_increment_step_pos: (group_increment_step IS NULL OR group_increment_step > 0)

📦 Table: public.orders
  - client_id integer
  - created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP
  - delivery_fee numeric NOT NULL DEFAULT 0
  - group_discount_total numeric NOT NULL DEFAULT 0
  - id integer NOT NULL DEFAULT nextval('orders_id_seq'::regclass)
  - is_delivered boolean NOT NULL DEFAULT false
  - is_notified boolean DEFAULT false
  - is_paid boolean DEFAULT false
  - is_ready boolean DEFAULT false
  - is_test boolean DEFAULT false
  - is_visible boolean DEFAULT true
  - paid_at timestamp with time zone
  - payment_method text
  - pre_group_total numeric
  - recorded_by text
  - total numeric
  - updated_at timestamp with time zone DEFAULT now()

🔗 Foreign Keys:
  - orders_client_id_fkey: client_id → public.clients(id) ON DELETE SET NULL

🔑 Unique Constraints:
  - orders_pkey: id [PRIMARY KEY]

📊 Indexes:
  - orders_pkey: id [btree] [UNIQUE]

✅ Check Constraints:
  - orders_delivery_fee_nonneg: (delivery_fee >= 0::numeric)

📦 Table: public.product_categories
  - category_id integer NOT NULL
  - created_at timestamp with time zone DEFAULT now()
  - product_id integer NOT NULL
  - sort_order integer DEFAULT 0
  - updated_at timestamp with time zone DEFAULT now()

🔗 Foreign Keys:
  - product_categories_category_id_fkey: category_id → public.categories(id) ON DELETE CASCADE
  - product_categories_product_id_fkey: product_id → public.products(id) ON DELETE CASCADE

🔑 Unique Constraints:
  - product_categories_pkey: (product_id, category_id) [PRIMARY KEY]

📊 Indexes:
  - product_categories_pkey: (product_id, category_id) [btree] [UNIQUE]

📦 Table: public.product_sale_groups
  - color text
  - created_at timestamp with time zone DEFAULT now()
  - label text
  - product_id integer NOT NULL
  - sale_group_id integer NOT NULL
  - updated_at timestamp with time zone DEFAULT now()

🔗 Foreign Keys:
  - product_sale_groups_product_id_fkey: product_id → public.products(id) ON DELETE CASCADE
  - product_sale_groups_sale_group_id_fkey: sale_group_id → public.sale_groups(id) ON DELETE CASCADE

🔑 Unique Constraints:
  - product_sale_groups_pkey: (product_id, sale_group_id) [PRIMARY KEY]

📊 Indexes:
  - product_sale_groups_pkey: (product_id, sale_group_id) [btree] [UNIQUE]

📦 Table: public.product_storage
  - product_id integer NOT NULL
  - storage_area_id integer
  - updated_at timestamp with time zone DEFAULT now()

🔗 Foreign Keys:
  - product_storage_product_id_fkey: product_id → public.products(id) ON DELETE CASCADE
  - product_storage_storage_area_id_fkey: storage_area_id → public.storage_areas(id) ON DELETE SET NULL

🔑 Unique Constraints:
  - product_storage_pkey: product_id [PRIMARY KEY]

📊 Indexes:
  - product_storage_pkey: product_id [btree] [UNIQUE]

📦 Table: public.products
  - created_at timestamp with time zone DEFAULT now()
  - description text
  - id integer NOT NULL DEFAULT nextval('products_id_seq'::regclass)
  - image text
  - in_stock boolean DEFAULT true
  - name character varying
  - price numeric
  - updated_at timestamp with time zone DEFAULT now()

🔑 Unique Constraints:
  - products_pkey: id [PRIMARY KEY]

📊 Indexes:
  - products_pkey: id [btree] [UNIQUE]

📦 Table: public.push_subscriptions
  - auth text NOT NULL
  - created_at timestamp with time zone NOT NULL DEFAULT now()
  - endpoint text NOT NULL
  - id integer NOT NULL
  - p256dh text NOT NULL
  - user_id integer NOT NULL

🔗 Foreign Keys:
  - push_subscriptions_user_id_fkey: user_id → public.users(id) ON DELETE CASCADE

🔑 Unique Constraints:
  - push_subscriptions_endpoint_unique: endpoint [UNIQUE]
  - push_subscriptions_pkey: id [PRIMARY KEY]

📊 Indexes:
  - idx_push_subscriptions_user_id: user_id [btree]
  - push_subscriptions_endpoint_unique: endpoint [btree] [UNIQUE]
  - push_subscriptions_pkey: id [btree] [UNIQUE]

📦 Table: public.sale_groups
  - created_at timestamp with time zone DEFAULT now()
  - id integer NOT NULL DEFAULT nextval('sale_groups_id_seq'::regclass)
  - image text
  - increment_step integer NOT NULL DEFAULT 1
  - name character varying
  - price numeric
  - quantity integer
  - sale_price numeric
  - updated_at timestamp with time zone DEFAULT now()

🔑 Unique Constraints:
  - sale_groups_pkey: id [PRIMARY KEY]

📊 Indexes:
  - sale_groups_pkey: id [btree] [UNIQUE]

✅ Check Constraints:
  - sale_groups_increment_step_pos: (increment_step > 0)

📦 Table: public.sales
  - created_at timestamp with time zone DEFAULT now()
  - id integer NOT NULL DEFAULT nextval('sales_id_seq'::regclass)
  - product_id integer
  - quantity integer
  - sale_price numeric
  - updated_at timestamp with time zone DEFAULT now()

🔗 Foreign Keys:
  - sales_product_id_fkey: product_id → public.products(id) ON DELETE CASCADE

🔑 Unique Constraints:
  - sales_pkey: id [PRIMARY KEY]

📊 Indexes:
  - sales_pkey: id [btree] [UNIQUE]

📦 Table: public.sessions
  - created_at timestamp with time zone NOT NULL DEFAULT now()
  - expires_at timestamp with time zone NOT NULL
  - id integer NOT NULL
  - ip_address inet
  - jwt_jti text NOT NULL
  - last_seen_at timestamp with time zone
  - revoked_at timestamp with time zone
  - revoked_reason text
  - session_key text NOT NULL
  - session_token_hash text NOT NULL
  - updated_at timestamp with time zone NOT NULL DEFAULT now()
  - user_agent text
  - user_id integer NOT NULL

🔗 Foreign Keys:
  - sessions_user_id_fkey: user_id → public.users(id) ON DELETE CASCADE

🔑 Unique Constraints:
  - sessions_jwt_jti_key: jwt_jti [UNIQUE]
  - sessions_pkey: id [PRIMARY KEY]
  - sessions_session_key_key: session_key [UNIQUE]
  - sessions_session_token_hash_key: session_token_hash [UNIQUE]

📊 Indexes:
  - idx_sessions_active_lookup: (user_id, revoked_at, expires_at) [btree]
  - idx_sessions_expires_at: expires_at [btree]
  - idx_sessions_user_id: user_id [btree]
  - sessions_jwt_jti_key: jwt_jti [btree] [UNIQUE]
  - sessions_pkey: id [btree] [UNIQUE]
  - sessions_session_key_key: session_key [btree] [UNIQUE]
  - sessions_session_token_hash_key: session_token_hash [btree] [UNIQUE]

📦 Table: public.storage_areas
  - created_at timestamp with time zone DEFAULT now()
  - id integer NOT NULL DEFAULT nextval('storage_areas_id_seq'::regclass)
  - name text NOT NULL
  - sort_order integer DEFAULT 0
  - updated_at timestamp with time zone DEFAULT now()

🔑 Unique Constraints:
  - storage_areas_name_key: name [UNIQUE]
  - storage_areas_pkey: id [PRIMARY KEY]

📊 Indexes:
  - storage_areas_name_key: name [btree] [UNIQUE]
  - storage_areas_pkey: id [btree] [UNIQUE]

📦 Table: public.users
  - id integer NOT NULL
  - role text NOT NULL

🔑 Unique Constraints:
  - users_pkey: id [PRIMARY KEY]
  - users_role_key: role [UNIQUE]

📊 Indexes:
  - users_pkey: id [btree] [UNIQUE]
  - users_role_key: role [btree] [UNIQUE]

✅ Check Constraints:
  - users_role_check: (role = ANY (ARRAY['admin'::text, 'superuser'::text, 'driver'::text]))
